<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <link rel="icon" href="./assets/icons/favicon.png" type="image/png">
    <title>Liturgical Services</title>
    <!-- Bootstrap CSS -->
    <!-- <link href="./assets/bootstrap/5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"> -->

    <!-- Optional: Bootstrap JS bundle (Popper + JS) -->
    <!-- <script src="./_assets/bootstrap/5.3.8/dist/js/bootstrap.bundle.min.js"></script> -->

    <!-- <link href="./assets/bootstrap-icons/1.13.1/font/bootstrap-icons.min.css" rel="stylesheet">
    <link href="./assets/bootstrap-icons/1.13.1/font/bootstrap-icons.woff" rel="stylesheet">
    <link href="./assets/bootstrap-icons/1.13.1/font/bootstrap-icons.woff2" rel="stylesheet"> -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/fonts/bootstrap-icons.woff" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/fonts/bootstrap-icons.woff2" rel="stylesheet"> -->
    <style>
    </style>
</head>
<body class="bg-light">

    <!-- <div class="container px-0 px-md-4 my-4">

        <div class="position-relative mb-4">
            <img src="./assets/icons/favicon.png"
                alt="Logo"
                width="110"
                height="40"
                class="position-absolute start-0 top-50 translate-middle-y">

            <h1 class="text-center mb-0">Liturgical Services</h1>
        </div>

        <div class="row g-3 mb-4">
            <div class="col-12 col-md-6">
                <select id="year" class="form-select"></select>
            </div>
            <div class="col-12 col-md-6">
                <select id="month" class="form-select"></select>
            </div>
        </div>

        <div id="list" class="list-group"></div>

    </div> -->

    
<!-- <div class="container-fluid px-0 my-4">
  <div class="px-md-4"> -->
    <div class="row g-3 mb-4">
      <div class="col-12 col-md-6">
        <select id="year" class="form-select"></select>
      </div>
      <div class="col-12 col-md-6">
        <select id="month" class="form-select"></select>
      </div>
    </div>
  <!-- </div>
</div> -->

    <script>
        const MONTHS = [
        "January","February","March","April","May","June",
        "July","August","September","October","November","December"
        ];

        const yearSelect = document.getElementById("year");
        const monthSelect = document.getElementById("month");
        const list = document.getElementById("list");

        let feasts = [];

        function initSelectors(data) {
        const years = [...new Set(data.map(f => f.date.slice(0,4)))].sort();

        years.forEach(y => {
            yearSelect.add(new Option(y, y));
        });

        MONTHS.forEach((m, i) => {
            monthSelect.add(new Option(m, String(i + 1).padStart(2, "0")));
        });

        const now = new Date();
        yearSelect.value = String(now.getFullYear());
        monthSelect.value = String(now.getMonth() + 1).padStart(2, "0");
        }

        function render() {
            list.innerHTML = "";

            const y = yearSelect.value;
            const m = monthSelect.value;

            const filtered = feasts
                .filter(f => f.date.startsWith(`${y}-${m}`))
                .sort((a, b) => a.date.localeCompare(b.date));

            if (!filtered.length) {
                list.innerHTML = `
                    <div class="text-center text-muted py-4">
                        No services found
                    </div>`;
                return;
            }

            // Grupare pe zi
            const groups = {};
            filtered.forEach(f => {
                const dayKey = f.date;
                if (!groups[dayKey]) groups[dayKey] = [];
                groups[dayKey].push(f);
            });

            Object.entries(groups).forEach(([date, items]) => {
                const [year, month, day] = date.split("-");
                const d = new Date(year, month - 1, day);

                const weekday = d.toLocaleDateString("en-US", { weekday: "long" });
                const monthName = MONTHS[d.getMonth()];
                const dayNum = day;

                const header = document.createElement("div");
                header.className = "list-group-item bg-secondary text-white fw-semibold";
                header.innerHTML = `${weekday}, ${monthName} ${dayNum}`;
                list.appendChild(header);

                items.forEach(f => {
                    const a = document.createElement("a");
                    a.href = f.file;
                    a.target = "_blank";
                    a.className = "list-group-item list-group-item-action py-3";
                    a.innerHTML = `<span class="font-medium">${f.title}</span>`;
                    list.appendChild(a);
                });
            });

        }

        fetch("Service_Texts/system/feasts.json")
        .then(r => r.json())
        .then(data => {
            feasts = data;
            initSelectors(feasts);
            render();
        });

        yearSelect.addEventListener("change", render);
        monthSelect.addEventListener("change", render);
    </script>

    </body>
</html>
